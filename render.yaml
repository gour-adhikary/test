services:
  - type: web
    name: test-backend-server
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start:direct
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
