name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Run linting
      run: npm run lint
      
    - name: Run tests
      run: npm test
      
    - name: Build application
      run: npm run build
      
    - name: Upload test coverage
      uses: codecov/codecov-action@v3
      if: matrix.node-version == '20.x'
      with:
        file: ./coverage/lcov.info
        fail_ci_if_error: false

  security:
    runs-on: ubuntu-latest
    needs: test
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Run security audit
      run: npm audit --audit-level high
      
    - name: Run npm audit fix
      run: npm audit fix --dry-run

  deploy:
    runs-on: ubuntu-latest
    needs: [test, security]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Deploy Status
      run: |
        echo "🚀 Tests passed! Ready for deployment."
        echo "📝 Render will auto-deploy this commit when connected to your repository."
        echo "🔗 Check your Render dashboard for deployment status."
        
    # Uncomment the following steps if you want to use GitHub secrets for automated deployment
    # - name: Deploy to Render
    #   uses: johnbeynon/render-deploy-action@v0.0.8
    #   with:
    #     service-id: ${{ secrets.RENDER_SERVICE_ID }}
    #     api-key: ${{ secrets.RENDER_API_KEY }}
    #     
    # - name: Wait for deployment
    #   run: |
    #     echo "Waiting for deployment to complete..."
    #     sleep 60
    #     
    # - name: Health Check
    #   run: |
    #     curl -f ${{ secrets.RENDER_SERVICE_URL }}/health || exit 1
    #     echo "Health check passed!"
        
    - name: Notify Success
      if: success()
      run: |
        echo "✅ CI Pipeline completed successfully!"
        echo "🚀 Code is ready for deployment!"
        echo "📋 Next steps:"
        echo "  1. Render will auto-deploy if connected to this repository"
        echo "  2. Check your Render dashboard for deployment status"
        echo "  3. Your service will be available once deployment completes"
